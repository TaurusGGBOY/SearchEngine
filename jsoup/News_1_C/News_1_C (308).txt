学习 前端技术需要掌握三门基本技术 使用工具 首先来写一个简单的 代码 通过如上可以发现 是网页内容的具体载体 接下来我们写一个 代码 第一个 代码如下 是由于创建网页的语言 我们通过使用 标记标签创建 文档来创建网页 代表超文本标记语言 它具有标记标签的集合 标签是由尖括号括起来的词 如 标签通常成对出现 例如 和 一对中的第一个标签是开始标签 第二个是结束标签 我们也可以称之为起始标签和闭合标签 起始标签是 结束标签是 标签之间是元素内容 即 标签和内容一起形成代码元素 一个 文件是有自己的固定结构的 代码讲解 注释 代码注释不仅能方便程序员回忆代码用途明晰思路 也可以让其他人快速读懂你的代码 方便合作开发 语法如下 语义化 通俗的说就是明白每个标签的用途 在什么情况下使用这个标签合适 你如网页的文章标题就得用标题标签 语义化的作用 作用 帮助浏览器正确地显示页面 世界存在许多不同的文档 只要了解文档类型 浏览器才能正确地显示文档 也有许多不同的版本 只有明白页面中使用的确切的 版本 浏览器才能完全正确地显示出 页面 这就是 的用处 它不是 标签 而是为浏览器提供一种信息 注意 常用声明如下 乱码解决 当页面出现乱码时 在在 标签之间添加 如果想在网页上显示文章 这是便需要 标签了 把文章的段落放到 标签中 语法如下 注意 在 文档中 标题 很重要 标题是通过 到 等标签进行定义的 定义最大标题 定义最小标题 观察各级标签 注意 有了段落标签和标题 现在如果想在一段话中特别强调某几个字 这时可以使用 和 标签 但是而二者在强调的语气上存在区别 使用标签范例 和 的区别 使用范例 如果希望不产生一个新的段落的情况下进行换行 请使用 标签 标签作用相当于 文档中的回车 使用范例如下 注意 是 还是 这二者很相似 但是在 以及未来的 版本中 不允许使用没有结束标签 闭合标签 的 元素 即使使用 在所有浏览器中都没有显示问题 但是使用 是更长远的保障 标签和 标签一样也是一个空标签 所以只有一个开始标签 没有结束标签 使用范例如下 标签在浏览器的默认样式线条比较粗 颜色为灰色 可能会觉得不够美观 在学习 后可以对其进行修改