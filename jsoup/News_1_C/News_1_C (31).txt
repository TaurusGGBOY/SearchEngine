这是我的 表格 内容部分加了个 用于后续获取数据覆盖区域 也就是所谓的动态刷新这个 的内容 先写一个 更新 的方法 数据是假的 这个看起来就是 的效果 也就是局部刷新 很简单 找到 的表格内容部分 直接用 替换 里的内容 好 下面我们来上一段真正的 代码 几个关键的地方 要么是 要么是 大多数时候两个都可以 就用 对应拼接好就行 后台的形参和 对应好 就能接收到 一般就是你请求的连接地址了 里的是 了 定义的是回台返回给你的数据格式 如果后台回传过来的是一个对象 一般这里写 这样用起来很方便 返回过来就是一个 对象 前端直接当做对象一样调用成员变量 如果回传过来的是 文本之类的字符串 你就写 就行了 拿到的是一个字符串 如果回传过来的是对象 但你依然在 上写了 那就要写一句 把这个字符串解析成 对象 这样 就成了可以直接调用成员变量的对象了 当你的指令发到后台 执行成功 并成功返回数据后 就会进入这个方法中 此时的 中就包含了你后端返回的值 在 里折腾吧 将 转换成对象后 就可以随便调用 所谓的动态刷新其实就是一个替换和覆盖的过程 如果是 的话 内部就是一些 的标签 在 里把获取到的数据重新拼接成和原来 中一样的数据格式 就是一堆 组成的字符串 然后把你的数据加进去 好 最后一步 使用 这个方法 通过 灯找到你想刷新数据的地方 直接替换掉里面的内容 动态刷新就实现了 代码如下 稍作修改就能使用 后端就是一个简单的 前端传过来的两个参数用形参来接受 路由对应的就是 