 是用 开发的一套通用的进程管理程序 能将一个普通的命令行进程变为后台 并监控进程状态 异常退出时能自动重启 只要在 的配置文件中 把要管理的进程的可执行文件的路径写进去即可 当子进程挂掉的时候 父进程可以准确获取子进程挂掉的信息的 可以选择是否自己启动和报警 还提供了一个功能 可以为 或者每个子进程 设置一个非 的 这个 就可以管理它对应的进程 标题写的是 其实管理其他的进程也是一样的道理 是基于 写的 所以我们用 来安装 我的操作目录是 操作用户是 如果你不是 身份 有的命令可能需要加上 提权 我的操作目录是 默认是没有配置文件的 所以如果不想从头 写 起 我们可以自己手动生成一份默认的配置文件 注 配置文件里面 前面加 表示注释 先贴一份自己用的 比较懒 带注释的也贴出来了 后面还有一份带讲解的配置文件 想深入了解的小伙伴可以取看一下 接下来是对配置文件的一些选项进行讲解了 说的不是很全面 大伙看看就好 老规矩 先贴一个自己用的 的这个启动命令默认是后台启动 不能监控后台程序 所以就会一直执行这个命令 不断的启动 就会报错 加上 这个参数可解决这问题 调用的配置文件的讲解 到这里基本就算是完成了 我们可以直接干掉 然后再启动 就可以了 它会自动把 启动起来 输入命令 可以进入交互界面 看到它监管的进程状态 如下 也可以直接用非交互的方式 在终端输入 进行管理 欢迎各位指出不足 