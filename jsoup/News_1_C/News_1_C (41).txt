上一篇文章介绍了单节点和双节点的 使用 也用 命令做了一些交互 这篇文章介绍一些 我至今经常用的命令 本文章不定期更新 通过 可以看到所有的 的命令 可以看到命令主要分两大类 和 在运行的时候需要加上 参数 将 解码成便于观看的交易信息格式 这个命令主要是将 命令返回的字节码解码成交易信息的格式 如 命令返回的信息 后面的参数 就是十六进制的字节码 手动生成区块的命令 手动生成区块其实和自动挖矿的原理是一样的 将交易打包 完成 交易等等 只不过我们可以指定生成区块的个数 自动挖矿挖的区块个数是不可控的 我前一篇文章有写 如果要自动挖矿必须更改 参数 从 改为 并且需要至少有一个节点与本地相连 才可以自动进行挖矿 获取当前区块链一共拥有的区块个数 这个个数是不包括创世区块的 所以链上如果只有一个创世区块 返回 当通过 看到一共有多少区块之后 可以用 命令来查看某个区块的唯一 值 注意创世区块的索引值为 当通过 拿到区块的 之后 可以通过 命令指定区块的 值 就可以查看到区块的具体信息 该命令可以通过指定 查询区块链上的任何一笔交易的内容 指的是区块中交易的 值 返回值是十六进制字节码 通过上述的 命令可以解码成能看懂的交易信息 值得注意的是 命令可以查看区块链上任意一笔交易的内容 不限于和本地钱包地址有关的交易 后面钱包命令的 也是查询交易内容 但是只能查询和本地钱包地址有关的交易 其他的交易是查询不到的 这个命令是控制是否要 自动进行挖矿 前面说到当更改了 参数为 之后 就开启了 自动挖矿 因为开启了 自动挖矿之后是不能再用 手动生成区块的 如果这个时候我们想手动挖矿 可以 置 将自动挖矿关闭 置 是将自动挖矿打开 此命令可以输出 账户下所有的地址 给 用户新建一个地址 如果不指定 是默认给 账户生成一个地址 查询本地钱包的某个交易 回显信息是可以读懂的交易信息格式 不需要进行解码 输出所有账户及相关余额 输出指定账户的前部交易信息 默认显示十条 这个命令我进行测试的时候 账户好像必须是 账户才能成功进行转账 不知道哪里出了问题不过没有深究 是地址 同上 做测试的时候 账户必须是 账户才能成功进行转账 后面大括号可以同时指定多个地址 从 账户给指定地址转钱 我到现在用的多的就是上面这些命令 而且经过我的测试 有一些命令好像还没有支持完全 希望以后能够逐渐完善