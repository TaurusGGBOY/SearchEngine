 包含访问 的接口并维护 来加快对 的访问 保证任何时候 集群中只有一个活跃 存贮所有 的寻址入口 实时监控 的上线和下线信息 并实时通知 存储 的 和 元数据 为 分配 负责 的负载均衡 发现失效的 并重新分配其上的 管理用户对 的增删改操作 维护 处理对这些 的 请求 负责切分在运行过程中变得过大的 自动把表水平划分成多个区域 每个 会保存一个表里面某段连续的数据 每个表一开始只有一个 随着数据不断插入表 不断增大 当增大到一个阀值的时候 就会等分会两个新的 裂变 当 中的行不断增多 就会有越来越多的 这样一张完整的表被保存在多个 上 与 一个 由多个 组成 一个 对应一个 列族 包括位于内存中的 和位于磁盘的 写操作先写入 当 中的数据达到某个阈值 会启动 进程写入 每次写入形成单独的一个 当 文件的数量增长到一定阈值后 系统会进行合并 在合并过程中会进行版本合并和删除工作 形成更大的 对于大范围合并 自动触发需要控制 当一个 所有 的大小和数量超过一定阈值后 会把当前的 分割为两个 并由 分配到相应的 服务器 实现负载均衡 客户端检索数据 先在 找 找不到再找 