--.将元组（学号：201215128；姓名：陈冬；性别：男；所在系：CP;年龄：18岁)插入到学生表中 --还可以这样添加 --插入选课记录 --对每一个系，求学生的平均年龄，并把结构数据存入数据库中  --order by:将查询结果按照一个或多个属性列的升序（ASC)或降序（desc),默认为升序 --group by :将查询结果按某一列的或多列的值分组 1.修改表： 格式如下： 注释：其中表名为要修改的基本表，add:用于增加新列、新的列级完整性约束条件、新的表级完整性约束条件 drop column子句:用于删除表中的列：如果指定级联（cascade)删除,则自动删除引用了该列的其他对象；如果指定了restrict短语，则如果该列被其他对象引用了，则管理员将拒绝删除该列； drop constraint子句：用于删除指定的完整性约束条件。 alter column子句：用于修改原有的列定义，包括修改列名和数据类型； 通配字符%和_: %:代表任意长度的字符串； _:代表任意单个字； --查询所有姓刘的学生的名字和学号、性别： --查询所有姓刘，且全名为两个子汉字的学生的姓名； --查询名字中第二个字为“洋"的学生的姓名和学号： --查询名字中所有不姓刘的学生的所有信息： --向student表增加“入学时间”列，其数据类型为date类型(添加列） --增加课程名称必须去唯一值的约束条件 --修改：将学生12306的年龄改为18 --将计算机科学系全体学生的成绩置空, --删除一个表： 注意：用restrict时表中有视图，则无法删除；用cascade时表中有视图，依然可以删除且视图也是自动删除； --删除名字带有“De_"的学生的信息 //使用 ESCAPE 关键字定义转义符。在模式中，当转义符置于通配符之前时，该通配符就解释为普通字符。例如，要搜索在任意位置包含字符串 5% 的字符串，请使用： WHERE ColumnA LIKE '%5/%%' ESCAPE '/' 即‘/’为转义字符，第二个‘%’为普通字符，第一、第三个为通配符。 //trim函数可以过滤指定的字符串： mysql 中有ltrim() rtrim() 和trim()函数，用来删除空格。 ltrim为删除左边的空格 rtrim为删除右边的空格 trim除了删除两边的空格，还有另外一种功能。不单单是空格，也可以是某个字符 --删除计算机系的学生的选课记录 