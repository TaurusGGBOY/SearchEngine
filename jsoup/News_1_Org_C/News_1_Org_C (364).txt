PI() #返回pi的值（圆周率）默认显示小数位数是6位 FLOOR(x) 返回小于x的最大整数（去掉小数取整）  CEILING(x)进一取整 ROUND（x,y）对操作苏进行四舍五入操作，保留小数点后y位，y为负数则保留小数点左边y位 RAND(x) 返回随机数 x相同时返回随机数相同 TURNCATE(x,y) 返回x保留y位小数 ABS（X） 返回x的绝对值 MOD(X，Y) 返回X被Y除以以后的余数，对带有小数的部分也成立  SQRT(x) 返回非负数x的二次平方根 SIGN(X) 返回参数的符号，x的值为正、0、负分别返回1、0、-1 POW(X,Y)、POWER(X,Y)、EXP(X) 幂函数 LOG(X)、LOG10(X) 对数运算 RADIANS(X)、DEGREES(X) 角度和弧度互相转换函数 SIN(X)、ASIN(X)正弦函数、反正弦函数 COS(X)、ACOS(X)余弦函数和反余弦函数 AVG（x）#平均 COUNT（col）#返回某一列中非控值的个数 MIN（col） #返回某一列最小值 MAX（col）#返回某一列最大值 SUM（col）#返回某一列的和  CONCAT（s1,s2,s3,...）#连接字符串s1,s2,s3,...  LTRIM(str) #去掉str开头的空格 RTRIM(str)#去掉str尾部的空格 TRIM(str)#去掉STR两头的空格  substring(str,开始截取位置loc,截取长度len)#loc=1:第一位,2第二个位置   YEAR(date) #返回date年份（1000-9999） MONTH(data)#返回date月份（1-12） DAY(data)#返回date日（1-31） HOUR(time) MINUTE(time) SECOND(time) DATE(datetime) TIME(datetima) as 取别名的关键字，可省略 select 数学函数（字段名） 别名，字段名 from 表明 group by 字段名 #如求各个班级的平均成绩  #如果使用分组函数时，不在分组中的字段必须放在group by 后面